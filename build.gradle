plugins {
    id 'java-library'

    id 'com.github.johnrengelman.shadow' version '7.1.0'

    id 'net.minecrell.plugin-yml.bukkit' version '0.3.0'
}

group 'com.github.kerminal'
version '1.0.0'

repositories {
    mavenCentral()
    mavenLocal()
    // spigot
    maven { url = 'https://maven.elmakers.com/repository/' }

    // command & inventory framework
    maven { url = 'https://jitpack.io/' }
}

dependencies {
    compileOnly 'org.spigotmc:spigot:1.8.8-R0.1-SNAPSHOT'

    implementation 'org.jetbrains:annotations:23.0.0'
    implementation 'com.github.HenryFabio:sql-provider:main-SNAPSHOT'
    implementation 'com.github.SaiintBrisson.command-framework:bukkit:1.2.0'

    compileOnly 'org.projectlombok:lombok:1.18.24'
    annotationProcessor 'org.projectlombok:lombok:1.18.24'
}

bukkit {
    name = 'Kerminal'
    main = 'com.github.kerminal.Kerminal'
    version = "${project.version}"
}

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

allprojects {
    libsDirName = 'C:/Users/rapha/Documents/Servidores/Servidor teste/plugins'
}

shadowJar {
    archiveName "${project.name}-${project.version}.jar"
}